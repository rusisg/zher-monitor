/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Чистые, плоские цвета для нашего интерфейса
        background: '#F9FAFB', // Слегка серовато-белый фон
        surface: '#FFFFFF', // Чисто белые карточки/панели
        primary: '#10B981', // Зеленый для акцентов (экология, успех)
        danger: '#EF4444', // Красный для кнопки "Сообщить о нарушении"
        textMain: '#1F2937', // Темно-серый для текста (не чисто черный)
        textMuted: '#6B7280', // Светло-серый для второстепенного текста
        borderFlat: '#E5E7EB', // Цвет для плоских границ
      },
      boxShadow: {
        // Убираем объемные тени, оставляем очень легкую плоскую тень для выделения панелей
        'flat': '0 1px 3px 0 rgba(0, 0, 0, 0.05), 0 1px 2px 0 rgba(0, 0, 0, 0.03)',
      }
    },
  },
  plugins: [],
}